# QA Sweep Bug Log — 2026-08-01

Companion to `2026-08-01-full-qa-sweep.md`. One row per finding; nothing stays `open` at close-out.

**Severity**
- **S1 blocker** — data loss/corruption, a wrong money figure, a lock bypassed, a page crashing to the error boundary. *Stop the sweep, fix immediately, re-verify, resume.*
- **S2 major** — a feature unusable or showing wrong data. *Log, finish the current task, fix before the next task.*
- **S3 minor** — copy, spacing, a missing empty state. *Batch; fix at Task 22 Step 3.*

**Status:** `open` → `fixed` / `wontfix (reason)` / `deferred (ticket)`

| # | Sev | Task | Area | Route / component | What happened | Expected | Status |
|---|-----|------|------|-------------------|---------------|----------|--------|
| 1 | S3 | 22 | Dead code | `components/ui/tooltip.tsx` | Zero importers across `app/`, `components/`, `lib/` (verified 2026-08-01) | Delete, or adopt it for the icon-only buttons that currently rely on `title=` | open |
| 2 | S3 | 22 | Dead code | `components/ui/field.tsx` | Zero importers (verified 2026-08-01) | Delete | open |
| 3 | S3 | 22 | Dead code | `app/(owner)/shops/actions.ts` · `resetEmployeePassword` | Exported server action with no UI caller (verified 2026-08-01). Credential resets go through `updateShopCredentials` | Delete the action, or wire it to a UI control if a reset-only path is wanted | open |
| 4 | S3 | 17 | Reports | `app/(owner)/reports/**` print header | Print header is the hardcoded string `Gerwin Trading — Report {from} to {to}` rather than `business_name` from Settings, unlike every other document letterhead | Read the name from `getBusinessIdentity` so a rename propagates | open |

---

## Pre-sweep baseline (2026-08-01)

Recorded before Task 1 so any regression during the sweep is attributable:

- `npx tsc --noEmit` — clean
- `npx next build` — compiled successfully
- `npm test` — **52 suites · 1,660 passed · 0 failed**
- `npm test -- --only=movements` — ledger reconciles (`Σ movements = stock_levels`)
- Staging data — 2 years (2024-07-31 → 2026-07-31), 10 branches, ~208k ledger rows, 334 MB / 500 MB
- Migrations applied through `0112`
